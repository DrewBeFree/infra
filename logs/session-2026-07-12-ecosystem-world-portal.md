# Ecosystem World Portal — 2026-07-12

Host/worktree: `/home/drew/GitHub/infra`
Branch: `dev`
Commit: pending closeout commit
Remote: `origin/dev`

Related repo touched: `/home/drew/GitHub/apps/drewbefree-command-center` on branch `dev`.

## Outcome

Built and deployed a new Atlas internal ecosystem world view at:

```text
http://atlas/ecosystem/world.html
https://portal.drewbefree.com/ecosystem/world.html
```

The world view is a registry-driven pan/zoom interface with spatial regions, search/filter, node detail drawers, a minimap, protected-link handling, and live metadata from `ecosystem.json`.

The deployed Atlas route loaded successfully and showed:

```text
66 visible / 66 total nodes • 2026-07-12
```

## Why this was done

Drew wanted a more visually appealing and intuitive way to navigate the many apps, agents, dashboards, services, businesses, and repos in the ecosystem. The existing tree was useful but not spatial/world-like enough for quick exploration.

Deployment was initially blocked by failing portal contract tests. The failures were a mix of stale route expectations and real registry drift.

## Files intentionally changed

Infra repo:

- `internal-portal/world.html`
  - New pan/zoom ecosystem world map.
  - Detail drawer/modal styling now uses a distinct gradient header, stronger shell, dashed action band, and separated detail sections so it does not look like another node card.
  - World regions are expandable/collapsible so the view starts as high-level regions instead of showing every node at once. Search auto-expands matching regions.
  - Added 3D globe-style background layers, a “Most active websites” strip, stronger unique region themes, and draggable region panels with connection lines that follow moved panels.
  - Featured active-site tiles open the detail drawer instead of navigating away, so sensitive/internal services such as John Share stay visible even without a public URL.
- `ecosystem.json`
  - Added missing required registry repos: `ai-chat-history`, `firecrawl`, `hermes-memory-archive`, `luke-codex-guide`, `personas-claude`, `selling-shit`, and `yes`.
  - Added John Share Atlas-local deploy target URL `http://127.0.0.1:8127` while keeping `liveUrls` empty because it is not public/browser-safe.
  - Updated `updatedAt` to `2026-07-12`.
  - Fixed Lead Desk, Leantime, and Hermes protected route metadata.
  - Added Atlas monitoring wiki docs link.
- `internal-portal/sync-links.json`
  - Added placeholder sync entries so repo count parity matches the registry.
- `internal-portal/launcher.html`
  - Added Ecosystem World launcher/rail links.
  - Added missing protected Hermes href.
- `internal-portal/index.html`
  - Added World navigation link.
- `internal-portal/README.md`
  - Documented world route shortcuts.
- `internal-portal/portal.test.mjs`
  - Updated route and sync expectations to match corrected Atlas/Tailscale source of truth.
- `internal-portal/changelog.html`
  - Regenerated by deploy/changelog workflow.

Command Center repo:

- `index.html`
  - Removed Poker Night and AI Dog Trainer cards from the public Command Center page, matching private registry `publicCommandCenter: false` expectations.
  - Updated public apps count from 7 to 5.
- `README.md`
  - Updated the public app index to match the currently exposed public page and noted hidden/private-registry entries.

## Commands run

```bash
cd /home/drew/GitHub/infra
git status --short --branch
git log --oneline -5
git diff --stat
node --test internal-portal/portal.test.mjs
python3 -m http.server 8766 --bind 127.0.0.1
./internal-portal/deploy.sh
```

Browser verification was run against both the local preview and deployed Atlas route. The final deployed browser check opened John Share from the active-site strip and confirmed the detail drawer opened in-place with 0 JS errors.

Observed test result:

```text
node --test internal-portal/portal.test.mjs
30 pass
0 fail
```

Observed deploy result:

```text
Wrote /home/drew/GitHub/infra/internal-portal/changelog.html
Projects: 53; local git repos: 34
Internal ecosystem portal synced to /opt/homelab-status-dashboard/ecosystem
```

Observed deployed registry summary:

```text
deployed updatedAt 2026-07-12
deployed repos/services/dashboards/docs 34 11 8 5
has world.html True
has ai-chat-history True
lead fallback http://100.71.165.80:3027/
leantime fallback http://100.71.165.80:8095/
has hermes route True
```

Observed browser result:

```text
http://atlas/ecosystem/world.html
66 visible / 66 total nodes • 2026-07-12
Search `lead` -> 2 visible nodes
Lead Desk detail drawer opened
Browser console: 0 JS errors
```

## Safety notes

- No secrets, `.env` files, private raw archives, or runtime DBs were read into logs or committed.
- `internal-portal/deploy.sh` updated `/opt/homelab-status-dashboard/ecosystem` and preserved the Atlas home redirect to `/ecosystem/launcher.html` with the old status dashboard at `/status/`.
- `internal-portal/deploy.sh` uses `rsync --delete` against `/opt/homelab-status-dashboard/ecosystem`; this was run only after the portal test suite passed.
- Existing unrelated dirty/untracked infra items were not part of the portal change and should not be swept into the portal commit:
  - `internal-portal/launcher-clean.html`
  - `logs/session-2026-07-09-monitoring-backups.md`
  - `monitoring/`
- Existing unrelated Command Center untracked file was not part of the public index cleanup:
  - `infrastructure-map.html`

## Commit and push

Pending at log creation. Recommended split:

```text
infra: feat: add ecosystem world portal
DrewBeFree-Command-Center: chore: align public command center app index
```

Push `dev` branches after commit if no additional review is needed.

## Next recommended step

Review the live world page visually from a normal browser/phone, then decide whether the world view should replace the tree as the main ecosystem entry point or remain a separate power-user navigation route.
